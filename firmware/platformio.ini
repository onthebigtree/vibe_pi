; ══════════════════════════════════════════════════════════════
; Vibe Pi — Multi-board PlatformIO Configuration
; ══════════════════════════════════════════════════════════════

[platformio]
src_dir = .
include_dir = .

; ── Shared settings ──
[common]
framework = arduino
monitor_speed = 115200
upload_speed = 921600
lib_deps =
    lvgl/lvgl@^9.2.0
    links2004/WebSockets@^2.6.1
    bblanchon/ArduinoJson@^7.3.0
    Wire
    SPI
    ESPmDNS
    WebServer
    Preferences
    Update
    HTTPClient

build_flags =
    -DLV_CONF_INCLUDE_SIMPLE
    -DLV_LVGL_H_INCLUDE_SIMPLE
    -I.
    -DCONFIG_ESP_TASK_WDT_TIMEOUT_S=30
    -Iinclude
    -Isrc
    -Ihal
    -Idrivers
    -Iboards

build_src_filter =
    +<src/>
    +<hal/*.cpp>
    +<drivers/display/*.cpp>
    +<drivers/touch/*.cpp>
    +<drivers/power/*.cpp>
    -<boards/>
    -<.git/>
    -<lib/>

; ── ESP32-S3 common ──
[esp32s3]
extends = common
platform = espressif32
board_build.mcu = esp32s3
board_build.f_cpu = 240000000L

; ══════════════════════════════════════════════════════════════
; BOARD: Waveshare 1.75" Round AMOLED (default)
; ══════════════════════════════════════════════════════════════
[env:waveshare-175-amoled]
extends = esp32s3
board = esp32-s3-devkitc-1
board_build.flash_size = 16MB
board_build.psram_type = opi
board_build.arduino.memory_type = qio_opi
build_flags =
    ${esp32s3.build_flags}
    -DBOARD_HAS_PSRAM
    -DBOARD_WAVESHARE_175_AMOLED
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
; This board uses CO5300 + CST9217; the GC9A01/CST816 drivers are dead code here
; (board_registry.cpp only references them under BOARD_GC9A01_ROUND_GENERIC).
build_src_filter =
    ${common.build_src_filter}
    -<drivers/display/gc9a01_driver.cpp>
    -<drivers/touch/cst816_driver.cpp>

; ══════════════════════════════════════════════════════════════
; BOARD: Generic 1.28" Round LCD (GC9A01 + CST816)
; ══════════════════════════════════════════════════════════════
; ══════════════════════════════════════════════════════════════
; HARDWARE TEST: Waveshare 1.75" — color fill + touch poll
; ══════════════════════════════════════════════════════════════
[env:hwtest]
extends = esp32s3
board = esp32-s3-devkitc-1
board_build.flash_size = 16MB
board_build.psram_type = opi
board_build.arduino.memory_type = qio_opi
build_flags =
    ${esp32s3.build_flags}
    -DBOARD_HAS_PSRAM
    -DBOARD_WAVESHARE_175_AMOLED
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DHWTEST
build_src_filter =
    +<src/test_hw.cpp>
    +<hal/*.cpp>
    +<drivers/display/*.cpp>
    +<drivers/touch/*.cpp>

; ══════════════════════════════════════════════════════════════
; BOARD: Generic 1.28" Round LCD (GC9A01 + CST816)
; ══════════════════════════════════════════════════════════════
[env:gc9a01-round-generic]
extends = esp32s3
board = esp32-s3-devkitc-1
board_build.flash_size = 8MB
board_build.psram_type = opi
board_build.arduino.memory_type = qio_opi
build_flags =
    ${esp32s3.build_flags}
    -DBOARD_HAS_PSRAM
    -DBOARD_GC9A01_ROUND_GENERIC
    -DARDUINO_USB_CDC_ON_BOOT=1
